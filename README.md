This code is a BASIC CALCULATOR PROGRAM in python . It takes two numbers and an arithmetic operator from the user. performs the selected operation.(import math )
imports the maths module.
In this program , math is not used, So it can be removed.
The program then process the input  using conditional statement(if-elif-else) and displays the result.
KEY FEATURES  :-
Accecpts two numerical inputs from the user
Supports:
  Addition(+)
  Substraction(-)
  Multiplication(*)
  Division(/)
Uses if-elif-else statement to determine the selected operation.
Displays the calculated result in a readable format.
Shows an error message if an invalid operator is entered.
PURPOSE:
    User input(input)
    Data type conversion(float())
    Condtional statement( if , elif , else )
    Arithmetic operators
    Output formating(print())
    
  It is a beginner-friendly program used to understand the fundamentals pf python programming and decision-making logic. 
  
  
